{
  "name": "astera-premium-shop",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/postbuild-static-pages.mjs",
    "preview": "vite preview",
    "data": "cd .. && python scripts/build_site_catalog_data.py"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
